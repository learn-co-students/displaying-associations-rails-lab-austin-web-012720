<h1>Artist Indies Page </h1>
<% @artists.each do |artist| %>
<%= link_to artist.name, artist_path(artist) %>
(<%= artist.song_count %> <%= "song".pluralize(artist.song_count) %>) <br>
<%end%>
<br>
<%=link_to "All Songs", songs_path %>
